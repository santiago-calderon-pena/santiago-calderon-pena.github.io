---
const { title, subtitle, date } = Astro.props;
---

<div class="flex">
  <div class="education__time">
    <span class="w-4 h-4 bg-primary block rounded-full mt-1"></span>
    <span class="education__line bg-primary block h-full w-[2px] translate-x-[7px]"></span>
  </div>
  <div class="experience__data bd-grid px-5">
    <div class="flex justify-between">
      <h3 class="font-semibold mb-1 text-xl">{title}</h3>
      <h3 class="font-semibold mb-1 text-right">{date}</h3>
    </div>
    
    <h2 class="font-semibold text-base">{subtitle}</h2>
    <p class="my-2 text-justify">
      <slot />
    </p>
  </div>
</div>
